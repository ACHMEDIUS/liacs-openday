'use client';

import FluidSimulation from '@/components/app/fluid-simulation';

export default function FluidSimulationPage() {
  return (
    <div className="relative h-screen w-screen overflow-hidden bg-black">
      <FluidSimulation />
    </div>
  );
}
